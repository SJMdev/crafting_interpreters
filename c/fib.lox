fun fib(n) {
    if (n < 2) return n;

    a  = fib(n - 2);
    b  = fib(n - 1);
    return a + b;
}

print fib(-1);



// fun recurse_once(value) {
//     if (value == 2) return "true";

//     if (value == 3) return recurse_once(2);
// }

// // print recurse_once(2);
// print recurse_once(3);